/**
 * Static "What's new" highlights shown on the welcome screen. Update this per
 * release — it is intentionally separate from the git history so releases can
 * surface a curated few lines.
 */
export const CHANGELOG = [
    '/folder to edit in a directory',
    '/model to switch models',
    '/settings to change your key',
    '/agents to run subagents',
    'ctrl+c twice to exit',
];
//# sourceMappingURL=changelog.js.map